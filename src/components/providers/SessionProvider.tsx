// import { useSession } from "next-auth/react"

// export default function SessionPorvider({ children }) {
//     const [session, loading] = useSession()

//     return (
//         <SessionContext.Provider value={{ session, loading }}>
//             {children}
//         </SessionContext.Provider>
//     )
// }